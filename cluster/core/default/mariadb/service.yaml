apiVersion: v1
kind: Service
metadata:
  name: &name mariadb
  labels: &labels
    app.kubernetes.io/name: *name
spec:
  selector: *labels
  ports:
    - name: &port tcp
      protocol: TCP
      port: 3306
      targetPort: *port
  type: ClusterIP
