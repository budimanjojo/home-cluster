apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-data-mariadb-0
  labels:
    app.kubernetes.io/name: redis
    snapshot.home.cluster/enabled: "true"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: rook-ceph-block
