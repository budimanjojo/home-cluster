apiVersion: v1
kind: ConfigMap
metadata:
  name: traefik-config
  namespace: ingress-system
  labels:
    app.kubernetes.io/name: traefik
data:
  dynamic.yaml: |
    http:
      # routers:
      #   redirect:
      #     rule: HostRegexp(`{host:.+}`)
      #     entryPoints:
      #     - http
      #     service: noop@internal
      #     middlewares:
      #     - redirect
