apiVersion: v1
kind: Service
metadata:
  name: kubed
  namespace: kube-system
  labels:
    app.kubernetes.io/name: kubed
spec:
  selector:
    app.kubernetes.io/name: kubed
  ports:
  - name: api
    protocol: TCP
    port: 443
    targetPort: kubed-port
  type: ClusterIP
