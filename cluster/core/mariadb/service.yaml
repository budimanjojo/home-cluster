apiVersion: v1
kind: Service
metadata:
  name: mariadb
  labels:
    app.kubernetes.io/name: mariadb
spec:
  selector:
    app.kubernetes.io/name: mariadb
  ports:
    - name: tcp
      protocol: TCP
      port: 3306
      targetPort: mariadb
  type: ClusterIP
