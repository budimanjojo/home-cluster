apiVersion: v1
kind: Service
metadata:
  name: mariadb-svc
  labels:
    app.kubernetes.io/name: mariadb
spec:
  selector:
    app.kubernetes.io/name: mariadb
  ports:
  - name: tcp
    protocol: TCP
    port: 3306
    targetPort: mariadb-port
  type: ClusterIP
