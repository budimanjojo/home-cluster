apiVersion: postgres-operator.crunchydata.com/v1beta1
kind: PostgresCluster
metadata:
  name: pgo-cluster
spec:
  metadata:
    annotations:
      reflector.v1.k8s.emberstack.com/reflection-allowed: "true"
  postgresVersion: 14
  patroni:
    dynamicConfiguration:
      synchronous_mode: true
      postgresql:
        synchronous_commit: on
        pg_hba:
          - hostnossl all all 10.244.0.0/16 md5
          - hostssl all all all md5
  instances:
    - name: instance1
      dataVolumeClaimSpec:
        storageClassName: rook-ceph-block
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 5Gi
    - name: instance2
      dataVolumeClaimSpec:
        storageClassName: rook-ceph-block
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 5Gi
  users:
    - name: fireflyiii
      databases:
        - fireflyiii
    - name: homeassistant
      databases:
        - homeassistant
    - name: vaultwarden
      databases:
        - vaultwarden
    - name: nextcloud
      databases:
        - nextcloud
    - name: authelia
      databases:
        - authelia
  backups:
    pgbackrest:
      configuration:
        - secret:
            name: pgo-cluster-s3-secrets
      global:
        repo1-retention-full: "14"
        repo1-retention-full-type: time
        repo2-retention-full: "7"
        repo2-retention-full-type: time
      repos:
        - name: repo1
          schedules:
            full: 0 1 * * 0
            incremental: 0 1 * * 1-6
          volume:
            volumeClaimSpec:
              accessModes:
                - ReadWriteMany
              resources:
                requests:
                  storage: 5Gi
              storageClassName: nfs-provisioner
        - name: repo2
          schedules:
            full: 0 5 * * 0
            incremental: 0 5 * * *
          s3:
            bucket: ${SECRET_PGO_BACKUP_S3_BUCKET}
            endpoint: ${SECRET_PGO_BACKUP_S3_ENDPOINT}
            region: ${SECRET_PGO_BACKUP_S3_REGION}
