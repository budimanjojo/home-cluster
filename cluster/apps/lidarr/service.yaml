apiVersion: v1
kind: Service
metadata:
  name: lidarr-svc
  labels:
    app.kubernetes.io/name: lidarr
spec:
  selector:
    app.kubernetes.io/name: lidarr
  ports:
    - name: http
      protocol: TCP
      port: 8686
      targetPort: lidarr-port
  type: ClusterIP
