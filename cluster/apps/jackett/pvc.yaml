apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jackett-config
  labels:
    app.kubernetes.io/name: jackett
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn
