apiVersion: v1
kind: Service
metadata:
  name: jackett
  labels:
    app.kubernetes.io/name: jackett
spec:
  selector:
    app.kubernetes.io/name: jackett
  ports:
    - name: http
      protocol: TCP
      port: 9117
      targetPort: http
  type: ClusterIP
