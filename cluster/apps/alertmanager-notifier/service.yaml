apiVersion: v1
kind: Service
metadata:
  name: alertmanager-notifier
  namespace: monitoring-system
  labels:
    app.kubernetes.io/name: alertmanager-notifier
spec:
  selector:
    app.kubernetes.io/name: alertmanager-notifier
  ports:
    - name: web
      protocol: TCP
      port: 8899
      targetPort: web
  type: ClusterIP
