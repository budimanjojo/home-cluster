apiVersion: v1
kind: Service
metadata:
  name: alertmanager-notifier-svc
  namespace: monitoring
  labels:
    app.kubernetes.io/name: alertmanager-notifier
spec:
  selector:
    app.kubernetes.io/name: alertmanager-notifier
  ports:
  - name: web-port
    protocol: TCP
    port: 8899
    targetPort: web-port
  type: ClusterIP
