apiVersion: v1
kind: Service
metadata:
  name: &name gatus
  namespace: monitoring-system
  labels: &labels
    app.kubernetes.io/name: *name
spec:
  selector: *labels
  ports:
    - name: &port http
      protocol: TCP
      port: 8080
      targetPort: *port
  type: ClusterIP
