apiVersion: v1
kind: Service
metadata:
  name: &name grafana
  namespace: monitoring-system
  labels: &labels
    app.kubernetes.io/name: *name
spec:
  selector: *labels
  ports:
    - name: &port http
      protocol: TCP
      port: 3000
      targetPort: *port
  type: ClusterIP
