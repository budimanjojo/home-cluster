apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: &name grafana
  namespace: monitoring-system
  labels: &labels
    app.kubernetes.io/name: *name
spec:
  selector:
    matchLabels: *labels
  endpoints:
    - interval: 15s
      port: http
      path: /metrics
      scheme: http
