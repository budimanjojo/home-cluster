apiVersion: v1
kind: Service
metadata:
  name: &name k8tz
  namespace: k8tz-system
  labels: &labels
    app.kubernetes.io/name: *name
spec:
  selector: *labels
  ports:
  - name: https
    protocol: TCP
    port: 443
    targetPort: https
  type: ClusterIP
