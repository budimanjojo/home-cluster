apiVersion: v1
kind: Service
metadata:
  name: radarr-svc
  labels:
    app.kubernetes.io/name: radarr
spec:
  selector:
    app.kubernetes.io/name: radarr
  ports:
    - name: http
      protocol: TCP
      port: 7878
      targetPort: radarr-port
  type: ClusterIP
