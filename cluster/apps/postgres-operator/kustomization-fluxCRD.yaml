apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: postgres-operator-postgres-operator
  namespace: flux-system
spec:
  path: ./cluster/apps/postgres-operator/postgres-operator
  sourceRef:
    kind: GitRepository
    name: flux-system
  interval: 30m
  prune: false
  wait: true
