apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: remark42-data
  labels:
    app.kubernetes.io/name: remark42
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 500Mi
  storageClassName: longhorn
