---
# yaml-language-server: $schema=https://schemas.budimanjojo.com/middleware_v1alpha1.json
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: immich-path-prefix
  labels:
    app.kubernetes.io/name: immich
spec:
  addPrefix:
    prefix: /api
