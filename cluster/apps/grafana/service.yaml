apiVersion: v1
kind: Service
metadata:
  name: grafana-svc
  namespace: monitoring-system
  labels:
    app.kubernetes.io/name: grafana
spec:
  selector:
    app.kubernetes.io/name: grafana
  ports:
    - name: http-port
      protocol: TCP
      port: 3000
      targetPort: http-port
  type: ClusterIP
