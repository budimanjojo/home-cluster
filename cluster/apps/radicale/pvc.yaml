apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: radicale-data
  labels:
    app.kubernetes.io/name: radicale
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: rook-ceph-block
