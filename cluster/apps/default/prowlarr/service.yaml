apiVersion: v1
kind: Service
metadata:
  name: &name prowlarr
  labels: &labels
    app.kubernetes.io/name: *name
spec:
  selector: *labels
  ports:
    - name: &port http
      protocol: TCP
      port: 9696
      targetPort: *port
  type: ClusterIP
