apiVersion: v1
kind: Service
metadata:
  name: &name bazarr
  labels: &labels
    app.kubernetes.io/name: *name
spec:
  selector: *labels
  ports:
    - name: &port http
      protocol: TCP
      port: 6767
      targetPort: *port
  type: ClusterIP
