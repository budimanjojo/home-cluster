apiVersion: v1
kind: Service
metadata:
  name: &name homeassistant
  labels: &labels
    app.kubernetes.io/name: *name
spec:
  selector: *labels
  ports:
    - name: &port http
      protocol: TCP
      port: 8123
      targetPort: *port
  type: ClusterIP
