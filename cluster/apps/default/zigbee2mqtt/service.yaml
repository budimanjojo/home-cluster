apiVersion: v1
kind: Service
metadata:
  name: &name zigbee2mqtt
  labels: &labels
    app.kubernetes.io/name: *name
spec:
  selector: *labels
  ports:
    - name: &port http
      protocol: TCP
      port: 8080
      targetPort: *port
  type: ClusterIP
