apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: default-vaultwarden
  namespace: flux-system
spec:
  path: ./cluster/apps/default/vaultwarden/base
  sourceRef:
    kind: GitRepository
    name: flux-system
  interval: 30m
  prune: true
  wait: true
  dependsOn:
    - name: default-postgrescluster
    - name: nfs-system-nfs-provisioner
