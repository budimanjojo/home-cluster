apiVersion: v1
kind: Service
metadata:
  name: &name radicale
  labels: &labels
    app.kubernetes.io/name: *name
spec:
  selector: *labels
  ports:
    - name: &port http
      protocol: TCP
      port: 5232
      targetPort: *port
  type: ClusterIP
