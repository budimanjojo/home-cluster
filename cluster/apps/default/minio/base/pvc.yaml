apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-nfs
  labels:
    app.kubernetes.io/name: minio
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 500Gi
  storageClassName: nfs-provisioner
