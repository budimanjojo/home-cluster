apiVersion: v1
kind: Service
metadata:
  name: &name sonarr
  labels: &labels
    app.kubernetes.io/name: *name
spec:
  selector: *labels
  ports:
    - name: &port http
      protocol: TCP
      port: 8989
      targetPort: *port
  type: ClusterIP
