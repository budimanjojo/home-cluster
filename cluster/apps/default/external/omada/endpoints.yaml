apiVersion: v1
kind: Endpoints
metadata:
  name: &name omada-controller
  labels:
    app.kubernetes.io/name: *name
subsets:
  - addresses:
      - ip: ${CONFIG_RASPBERRY_PI_IP_ADDRESS}
    ports:
      - name: http
        port: 8043
        protocol: TCP
