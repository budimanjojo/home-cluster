apiVersion: v1
kind: Endpoints
metadata:
  name: fullykioskbrowser
subsets:
  - addresses:
      - ip: ${CONFIG_DASHBOARD_TABLET_IP_ADDRESS}
    ports:
      - name: http
        port: 2323
        protocol: TCP
