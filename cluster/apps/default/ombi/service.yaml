apiVersion: v1
kind: Service
metadata:
  name: &name ombi
  labels: &labels
    app.kubernetes.io/name: *name
spec:
  selector: *labels
  ports:
    - name: &port http
      protocol: TCP
      port: 3579
      targetPort: *port
  type: ClusterIP
