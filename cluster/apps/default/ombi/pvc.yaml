apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ombi-config
  labels:
    app.kubernetes.io/name: ombi
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
  storageClassName: rook-ceph-block
