apiVersion: v1
kind: Service
metadata:
  name: remark42
  labels:
    app.kubernetes.io/name: remark42
spec:
  selector:
    app.kubernetes.io/name: remark42
  ports:
  - name: http
    protocol: TCP
    port: 8080
    targetPort: 8080
  type: ClusterIP
