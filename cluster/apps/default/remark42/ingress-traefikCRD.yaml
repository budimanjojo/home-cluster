apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: &name remark42
  labels:
    app.kubernetes.io/name: *name
spec:
  entryPoints:
    - https
  routes:
    - kind: Rule
      match: Host(`comments.${SECRET_DOMAIN_1}`)
      middlewares:
        - name: site-headers@file
      services:
        - name: *name
          port: 8080
          namespace: default
  tls:
    secretName: ${SECRET_CERT_NAME_1}-prod-tls
