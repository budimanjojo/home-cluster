apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: remark42-data
  labels:
    app.kubernetes.io/name: remark42
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
  storageClassName: rook-ceph-block
