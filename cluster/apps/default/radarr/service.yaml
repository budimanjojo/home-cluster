apiVersion: v1
kind: Service
metadata:
  name: &name radarr
  labels: &labels
    app.kubernetes.io/name: *name
spec:
  selector: *labels
  ports:
    - name: &port http
      protocol: TCP
      port: 7878
      targetPort: *port
  type: ClusterIP
