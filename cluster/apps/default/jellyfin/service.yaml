apiVersion: v1
kind: Service
metadata:
  name: &name jellyfin
  labels: &labels
    app.kubernetes.io/name: *name
spec:
  selector: *labels
  ports:
    - name: &port http
      protocol: TCP
      port: 8096
      targetPort: *port
  type: ClusterIP
