apiVersion: v1
kind: Service
metadata:
  name: &name mosquitto
  labels: &labels
    app.kubernetes.io/name: *name
spec:
  selector: *labels
  ports:
    - name: &port mqtt
      protocol: TCP
      port: 1883
      targetPort: *port
  type: ClusterIP
