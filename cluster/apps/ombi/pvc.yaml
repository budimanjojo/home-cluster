apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ombi-config
  labels:
    app.kubernetes.io/name: ombi
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs-provisioner
